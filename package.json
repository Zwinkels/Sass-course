{
  "name": "Sass-course",
  "version": "1.0.0",
  "description": "The starter course for Sass",
  "main": "index.js",
  "scripts": {
    "start": "npm run gulp && concurrently \"npm run gulp-watch\" \"npm run lite\"",
    "lite": "lite-server",
    "gulp": "gulp",
    "gulp-watch": "gulp startwatch"
  },
  "devDependencies": {
    "concurrently": "^3.5.0",
    "gulp": "^3.9.1",
    "gulp-sass": "^3.1.0",
    "gulp-watch": "^4.3.11",
    "lite-server": "^2.3.0"
  }
}
